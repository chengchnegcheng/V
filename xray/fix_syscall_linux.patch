--- xray/manager.go.orig
+++ xray/manager.go
@@ -1154,11 +1154,7 @@
 	// 启动xray进程
 	cmd := exec.Command(execPath, "-config", configPath)
 
-	// 在 Windows 上设置特定的进程属性
-	if runtime.GOOS == "windows" {
-		// 避免显示命令行窗口
-		cmd.SysProcAttr = &syscall.SysProcAttr{
-			HideWindow: true,
-		}
-	}
+	// 设置平台通用的进程属性
+	cmd.SysProcAttr = &syscall.SysProcAttr{}
 
 	// 设置输出 