// 适用于Windows的syscall配置
package xray

import (
	"os/exec"
	"syscall"
)

// 在Windows环境下的configureProcessAttributes
func configureProcessAttributes(cmd *exec.Cmd) {
	// Windows系统下的进程属性设置
	cmd.SysProcAttr = &syscall.SysProcAttr{
		HideWindow: true,
	}
} 