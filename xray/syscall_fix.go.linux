// 适用于Linux的syscall配置
package xray

import (
	"os/exec"
	"syscall"
)

// 在Linux环境下的configureProcessAttributes
func configureProcessAttributes(cmd *exec.Cmd) {
	// Linux系统下的进程属性设置
	cmd.SysProcAttr = &syscall.SysProcAttr{}
}